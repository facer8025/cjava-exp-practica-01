spring:
  application:
    name: cjava-matricula-respaldo
  data:
    mongodb:
      host: localhost
      port: 27017
      database: escuela
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

server:
  port: 8070
